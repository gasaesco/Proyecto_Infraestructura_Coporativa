define host {
    use                     linux-server
    host_name               dns
    alias                   Servidor DNS
    address                 172.30.0.10
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               suricata
    alias                   IDS Suricata
    address                 172.30.0.30
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               nagios
    alias                   Monitor Nagios
    address                 172.30.0.40
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               kali
    alias                   Kali Linux
    address                 172.30.0.20
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               mailserver
    alias                   Mail Server
    address                 172.30.0.4  
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               roundcube
    alias                   RoundCube
    address                 172.30.0.5
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               openvpn
    alias                   OpenVPN
    address                 172.30.0.3
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               mariadb
    alias                   MariaDB
    address                 172.30.0.2
    check_command           check-host-alive
}





