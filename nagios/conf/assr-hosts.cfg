define host {
    use                     linux-server
    host_name               dns
    alias                   Servidor DNS
    address                 172.30.0.10
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               suricata
    alias                   IDS Suricata
    address                 172.30.0.30
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               nagios
    alias                   Monitor Nagios
    address                 172.30.0.40
    check_command           check-host-alive
}

define host {
    use                     linux-server
    host_name               kali
    alias                   Kali Linux
    address                 172.30.0.20
    check_command           check-host-alive
}
